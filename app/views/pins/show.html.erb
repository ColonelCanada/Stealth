<div class="jumbotron">	
	<p id="notice"><%= notice %></p>

	<p>
	  <strong>Post:</strong>
	  <%= @pin.description %>
		<%= image_tag @pin.image.url(:medium) %>
	</p>
	<%= link_to 'Back', pins_path %>
</div>